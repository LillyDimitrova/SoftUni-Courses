spring:
  datasource:
      driverClassName: org.hsqldb.jdbc.JDBCDriver
      password: root123
      username: root
  jpa:
    hibernate:
      ddl-auto: create-drop

  mail:
    from: me@localhost
    host: localhost
    password: ''
    port: 1025
    protocol: smtp
    smtp:
      auth: false
      starttls:
        enable: false
    username: ''
  sql:
    init:
      mode: always